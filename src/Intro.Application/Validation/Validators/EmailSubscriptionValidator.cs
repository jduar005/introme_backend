// TODO: below is an example of fluent validation usage

namespace Intro.Application.Validation.Validators
{
    //using FluentValidation;

    //public class EmailSubscriptionValidator : AbstractValidator<EmailSubscription>
    //{
    //    public EmailSubscriptionValidator()
    //    {
    //        this.RuleFor(s => s.Email).NotEmpty().WithMessage("Email.Required");

    //        this.RuleFor(s => s.Email).EmailAddress().When(s => !string.IsNullOrEmpty(s.Email)).WithMessage("Email.Invalid");
    //    }
    //}
}
